<div class="w-full flex items-center justify-center py-6">
  <%= link_to "new list", new_list_path, data: { "turbo-frame": "_top" }, class: 'text-center bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white py-2 px-4 border border-blue-500 hover:border-transparent rounded' %>
</div>

<% @lists.each do |list| %>
  <div>
    <%= link_to list.description, list_path(list) %> || <%= list.items.count %> items ||
    <% if can? :manage, list %>
      <%= link_to t('edit'), edit_list_path(list) %> ||
      <%= button_to t('delete'), list_path(list), method: :delete, form: { data: { turbo_confirm: t('confirm_delete') } } %>
    <% end %>
  </div>
<% end %>
